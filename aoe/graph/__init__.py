"""
graph package — LangGraph node wrappers, edge routing, and compiled workflow.

All graph related things are in graph/workflow.py. External callers must go through
middleware/handle.py; the compiled graph is never invoked directly.
"""
